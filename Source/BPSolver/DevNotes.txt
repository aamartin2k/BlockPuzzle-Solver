Refactor:
  Class Piece:
   --Hacer Matrix propiedad private set;
   --  Implica cambios en Factory NewSolver CreatePieceSet()
   -- Hace instance method que devuelva coord reales en basea insert point (ins point)

   --Establecer primer elem como default en tsbutoons piece

   --Impl Modo Gridcelldrawing _  click nextpiece 
     chequear cambios de estado:
	  Deleting a NextPiece, mantener Delete
	  Deleting a ToolPiecde, cambiar a PieceSetting
Controller
Implementar Play, borrar nextPiece
Modular opciones de chequeo
eliminar chequeo de completamiento de Draw
--Implementar cloning de gameStatus
--Implementar actualizar lista de hijos
Implementar descripcion del movimiento
Mejorar impl de estadisticas, quitar de GameStatus y pasar a Meta


Analizar implicaciones de Jugada y Undo
 ?al crearse nuevo estado por jugada se resetea UndoStack

 --en cada cambio de estado se resetea Stack
 --al hacer jugada en ultimo nodo, por defecto se crea un estado hijo y se avanza,

 Implementacion de Soluciones
  --Cambiar GameStatus, lista NextPieces por Diccionario
  --Cambiar prop Id a public ser\t, eliminar metodo  SetId

  Cambiar definit de CloneGameStatus de Controller a Solver
 
 Analizar Convertir Controller/Solver a Static

 --Aplicar completamiento y eliminacion al final de cada moviminto, de lo 
  --contrario se suma la cantidad de filas completadas.
Analizar en Relacion con Preferencia que Param Vecinos debe contar como vecinos los bordes

--Implementar eliminaciopn SIMULTANEA de filas y columnas. En caso de coincidencia, hay error

Establecer limite de iteraciones en cmo

Implemmentar NextPiece como Queue pra uso en Solver
--
Implementar construccion completa del tree, modificr ProcessMoveNuevo
 para hacr lista de movs, clon status com movimiento asignado
  y dejar a posterior su aplicacion y evaluacions

  Optimizar proc iterativo, retornar estadisticas *(tiempo y cant nodos)

  Al preparar la solucion, sustituir Cloned Root por original y
   reconstruir los estados de las soluciones aplicandop movs al original, para
   recuperar inf color
  Si existen soluciones con complet,  no retornar las otras

  Eliminar de Interface e eimplement: 
    Action<int[]> Out_SelectRows { get; set; }
	Action<int[]> Out_SelectColumns { get; set; }
	 la animacoin etc debe sr resuelta en GUI

	  Terminado y comprobado impl de Cargar formato viejo y salvar en nuevo, para Mantest
	  terminar, revisar cambios en UpdateBoard (None fallo), incorporar
 nuevos datos del documento en Winform
  Implementando cambios de Interface, con prueba manual
  --void In_NewFile();
  --void In_CloseFile();
   void In_LoadFile(string file);
   --void In_SaveFile();
   --void In_SaveFileAs(string file);

 
Revision y Prueba de comp func
	-Document
	-Tree 
	-Game
	-Solver

